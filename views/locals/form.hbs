<div class="row justify-content-md-center">
  <div class="col-xs-12 col-sm-12 col-md-4">
    <form action="/local" method="POST">
      <div class="form-group">
        <div class="custom-fi">
          {{!-- {{#if local.isNew}}
            <form action="/local" method="POST">
            {{else}}
            <form action="/local/{{local._id}}" method="POST">
          {{/if}} --}}
          <label class="custom-file-ll" for="nombre">Name</label>
          <input type="text" class="name" name="name" value="{{local.name}}">
          <label class="custom-file-bel" for="address">Address</label>
          <input type="text" class="address" name="address" value="{{local.address}}">
          <label class="custom-file-bel" for="phone">Phone</label>
          <input type="number" class="phone" name="phone" value="{{local.phone}}">
          {{!-- <input type="text" class="cat"> --}}
        </div>
        <label for="">Category</label>
        <select name="food" id="">
            {{#each foods as |food|}}
            <option value="{{food}}" {{#hasCategory ../local.category.food food}} selected{{/hasCategory}}>{{food}}</option>
            {{/each}}
        </select>
        <select name="music" id="">
          {{#each musics as |music|}}
          <option value="{{music}}"{{#hasCategory ../local.category.music music}} selected {{/hasCategory}}>{{music}}</option>
          {{/each}}
        </select>
        <div>
          <label class="custom-file-bel" for="phone">localType</label>
          <select name="localType" id="">
            {{#each places as |place|}}
            <option value="{{place}}"{{#hasCategory ../local.localType place}} selected {{/hasCategory}}>{{place}}</option>
            {{/each}}
          </select>
        </div>
      </div>
          {{local}}
      <div class="form-actions">
        <button type="submit" class="btn btn-primary btn-block">Create</button>
      </div>
    </form>
  </div>
</div>